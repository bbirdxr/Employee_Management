<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.employee.mapper.LeaveMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.employee.entity.Leave">
        <id column="id" property="id" />
        <result column="employee_id" property="employeeId" />
        <result column="department_id" property="departmentId" />
        <result column="start_date" property="startDate" />
        <result column="end_date" property="endDate" />
        <result column="leave_type" property="leaveType" />
        <result column="leave_reason" property="leaveReason" />
        <result column="approver_id" property="approverId" />
        <result column="approve_status" property="approveStatus" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, employee_id, department_id, start_date, end_date, leave_type, leave_reason, approver_id, approve_status, create_time, update_time
    </sql>

    <select id="selectById" resultType="com.example.employee.entity.Attendance">
        select id, employee_id, department_id, start_date, end_date, leave_type, leave_reason, approver_id, approve_status, create_time, update_time
        from leave
        where id = #{id}
    </select>

    <insert id="insert" parameterType="com.example.employee.entity.Attendance">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into leave (employee_id, department_id, start_date, end_date, leave_type, leave_reason)
        values (#{employeeId,jdbcType=BIGINT}, #{departmentId,jdbcType=BIGINT}, #{startDate,jdbcType=DATE},
        #{endDate,jdbcType=DATE}, #{leaveType,jdbcType=TINYINT}, #{leaveReason,jdbcType=VARCHAR})
    </insert>

    <delete id="deleteById" parameterType="java.lang.Long">
        delete from leave
        where id = #{id,jdbcType=BIGINT}
    </delete>

    <update id="update" parameterType="com.example.employee.model.dto.LeaveUpdateDTO">
        update leave
        set start_date = #{startDate,jdbcType=DATE},
            end_date = #{endDate,jdbcType=DATE},
            leave_type = #{leaveType,jdbcType=TINYINT},
            leave_reason = #{leaveReason,jdbcType=VARCHAR}
        where id = #{id,jdbcType=BIGINT}
    </update>

    <update id="approveById">
        update leave
        set approver_id = #{approverId,jdbcType=BIGINT},
            approve_status = #{approveStatus,jdbcType=TINYINT}
        where id = #{id,jdbcType=BIGINT}
    </update>

    <select id="selectRecentLeaveByEmployeeId" parameterType="java.lang.Long">
        select id, employee_id, department_id, start_date, end_date, leave_type, leave_reason, approver_id, approve_status, create_time, update_time
        from leave
        where employee_id = #{employeeId,jdbcType=BIGINT}
        order by create_time desc
        limit 1
    </select>

    <select id="selectByEmployeeId" parameterType="java.lang.Long">
        select
        <include refid="Base_Column_List" />
        from leave
        where employee_id = #{employeeId,jdbcType=BIGINT}
    </select>



</mapper>
